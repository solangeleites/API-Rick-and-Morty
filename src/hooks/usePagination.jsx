import React, { useState } from 'react';

const usePagination = (data, itemsPage) => {
  const [currentPage, setCurrentPage] = useState(1);

  return <div>usePagination</div>;
};

export default usePagination;
